<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    {{#if title}}
        <title>{{{title}}}</title>
    {{/if}}
    {{#if description}}
        <meta name="description" content="{{{description}}}">
    {{/if}}
    <link rel="stylesheet" type="text/css" href="/assets/bootstrap/dist/css/bootstrap.min.css">
    <script type="text/javascript" src="/assets/requirejs/require.js"></script>
    <script>requirejs(['./app/common'], function (common) { requirejs(['app/{{{requireModule}}}']); });</script>
</head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="javascript:void(0)">I.W.API</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        {{{echoNav}}}
                    </ul>
                </div>
            </nav>
        </header>
        <main>
        <div class="container-fluid">
            {{{body}}}
        </div>
        </main>
        <footer>
            <nav class="navbar navbar-light bg-light">
                <p>Powered by Fabio Ansaldi</p>
            </nav>
        </footer>
    </body>
</html>