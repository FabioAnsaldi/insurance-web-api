<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    {{#if title}}
        <title>{{{title}}}</title>
    {{/if}}
    {{#if description}}
        <meta name="description" content="{{{description}}}">
    {{/if}}
</head>
    <body>
    {{{body}}}
    </body>
</html>